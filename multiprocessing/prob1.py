#MPI is designed for distributed systems, hence the message passing utility
#As far as I can understand, POSIX threads and OpenMP work only on a single machine. I've used OpenMP before (for PE 529, the 10-substrings problem).
